name: Test

on: [push]

jobs: 
  test_build:
    runs-on: ubuntu-latest

    steps:
      - name: ls
        run: |
          echo "hola mundo"  >> hola.txt
          cat hola.txt

      - name: ls
        uses: ./.github/actions/my-action